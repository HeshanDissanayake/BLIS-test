experiment -1: kernel V1(access B first)
experiment -2: kernel V1(access B first)
experiment -3: to show that just increasing the register tile does not scale up well

# retire kernel V1
experiment -4: kernel V2(access A first), to test behaviour of increasing Kc
experiment -5: mr=4   kc =128(best for L1) used to see the sensitivity of mc for L2
experiment -6: mr=8   kc =64 (best for L1) used to see the sensitivity of mc for L2
experiment -7: mr=16  kc =32 (best for L1) used to see the sensitivity of mc for L2 wrong KC

# restrict only to single matrix size 1024
experiment -8:  mr=4  test Kc sensitivity
experiment -9:  mr=8  test Kc sensitivity
experiment -10: mr=16 test Kc sensitivity
experiment -11: sum of 8, 9, 10

# restrict only to single matrix size 1024
experiment -12:  mr=4  test Kc sensitivity with cycle count
experiment -13:  mr=8  test Kc sensitivity with cycle count
experiment -14: mr=16 test Kc sensitivity with cycle count
experiment -15: sum of 12, 13, 14

# system test test differentiating mr and nr 
experiment -16: mr=4  nr=4  test Kc sensitivity with cycle count

# differetiating mr and nr for all 1,2,4,6,8,12,16
experiment -17:  test Kc sensitivity with cycle count & memtrace

experiment -18:  same as experiment -17 changed L1 parameters

experiment -19:  same as experiment -17 ,now the KC formula is changed to have CAr +CBr = WL1

#!!!!!!!!!!!!!!!!!!!!!!!!
retire experiment 16- 19 since KC equation is wrong.

# mrxnr comination for mat size 512 with corrected KC formula
experiment -20: L1 size 32KB, LW1 = 16, ASC=8

experiment -21: L1 size 32KB, LW1 = 32, ASC=16 ,  keep the same cache size as exp-20 but changing Lw and ASC

experiment -22: testing fine changed mc and nc

# test extension for 2 to 16 with full sweep
experiment -23: has full 2 -16, used for the linux builds
    experiment -24: all MR;  NR:2,4
    experiment -25: all MR;  NR:6,8
    experiment -26: all MR;  NR:10,12
    experiment -27: all MR;  NR:14,16

