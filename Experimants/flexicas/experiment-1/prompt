I want to create a new csv analys and plotting program in python.

inputs:
    csv_file: csv file name
    root:dir
    x: in plot x dimetion
    y: in plot y dimention
    subplot_x: subplot x dimention
    subplot_y: subplot y dimention

funtionality:
    this program has to scan the given root dir and find all the csv files in the name of csv_file, which means at the all leaf nodes there is a csv.
    each the directory structure defines the all dimentions values, for example:
        experiment-1/L1_32/LW16/ASC8/MC4096/KC32/NC4096/terminatedl1d_set_utilization_BL_32KB.csv

        root: experiment-1
        dims:
            L1 = 32
            LW = 16
            ASC = 8
            MC = 4096
            KC = 32
            NC = 4096
        (note that it could contain either _ or not)
    
    given the x and y (find them inside the csv) it should plot it. prompt an erroe if not found
    each plot should be arranged in a 2D configurations accoding to subplot_x and subplot_y selected from dims. prompt an erroe if not found
    then there should be a single image with x,y,subplot_x,subplot_y. lets say it is img_out
    the remaining any amount of dimention should be used to make a directory heirachy and place the img_outs indside those directories


here are your answers

Dimension Parsing Logic:
you should smartly parse both ways, genrely there will be a lable and a neumeric either with not space or _

Should the script strictly follow a rule like: "Treat all leading alphabetic characters as the Key and the trailing numeric digits as the Value, ignoring a single optional underscore in between"? yes
Are the values always numbers, or could they be strings (e.g. 32KB)? always numbers


Output Structure:

You mentioned creating a directory hierarchy for the img_out based on the remaining dimensions. Should this hierarchy be created inside a specific new output folder (e.g., ./plots/), or mixed into the existing source folders?
What should the filename of the generated image be? (e.g., plot.png?) this is good and create a specific output folder


Plotting Details:

Do x and y inputs refer to the column headers inside the CSV files? yes, prompt a error if not found
What type of plot do you prefer? (e.g., Line plot, Scatter plot, Bar chart?) lets put a option for that

If a specific (subplot_x, subplot_y) combination is missing from the data, should the script leave that subplot empty? remember they should be taken form directory structure(dims) and exit if there are not combinations

CSV Discovery:

When you say "find all the csv files in the name of csv_file", do you mean exactly matching that filename (e.g. find . -name "my_data.csv"), or files containing that string? yes



,Accesses,Hits,Misses,Evictions,WaysUsed,HitRate,EvictionRate



python3 analyse.py \
  --root "/home/heshds/working_dir/BLIS-test/Experimants/flexicas/experiment-1" \
  --csv_file "terminatedl1d_set_utilization_BL_32KB.csv" \
  --x "Set" \
  --y "Misses" \
  --subplot_x "NC" \
  --subplot_y "KC" \
  --output_dir "plots_result"